# TicketScout — Cloudflare Pages redirects
# NOTE: Concert/artist pages are handled by Pages Functions, not redirects.
# See functions/concert/[slug].js for the concert URL router.
# Future category routers should follow the same pattern.

# Redirect /concerts to homepage (category browsing handled there for now)
/concerts /  302
/theatre  /  302
/football /  302
